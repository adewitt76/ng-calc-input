import { NgCalcInputDirective } from './ng-calc-input.directive';

describe('NgCalcInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NgCalcInputDirective();
    expect(directive).toBeTruthy();
  });
});
